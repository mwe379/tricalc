import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mwe379.triathloncalculator',
  appName: 'Triathlon Calculator',
  webDir: 'dist'
};

export default config;
